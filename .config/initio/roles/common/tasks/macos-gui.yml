---

- homebrew:
    name: "{{item}}"
    state: present
  with_items:
    - gpg-suite

- homebrew_cask:
    name: "{{item}}"
    state: present
  with_items:
    - disk-inventory-x
    - Firefox
    - google-chrome
    - keka
    - keybase
    - veracrypt
    - vlc
